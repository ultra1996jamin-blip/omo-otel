# 심사 제출용 소스 (본인 직접 작성/수정)

`omo-source-code-guide.md`(부록 A 포함)에서 "본인 직접 작성/수정"으로 분류한 파일들을 원본 레포 경로 그대로 모아둔 폴더다. git으로 통째로 받을 수 없는 심사자에게 이 폴더만 압축해서 전달하면 된다(예: `Compress-Archive -Path deploy/observability/submission/* -DestinationPath omo-otel-authored-sources.zip`).

## 구성 (42파일)

| 경로 | 파일 수 | 구분 |
|---|---:|---|
| `packages/otel-core/` | 23 | 전량 신규 작성 (패키지 자체가 이번 과제에서 신규 생성) |
| `packages/omo-opencode/src/{plugin,shared,config/schema}` | 13 | 계측 연동 (신규 9 + 기존 파일 수정 4: `event.ts`·`tool-execute-before.ts`·`tool-execute-after.ts`·`config/schema/otel.ts`) |
| `deploy/observability/grafana/dashboards/*.json` | 5 | 대시보드 구성/수정 |
| `deploy/plugin-airgap/README.md` | 1 | 폐쇄망 설치 절차 구성 |

파일별 정확한 역할·구분은 `omo-source-code-guide.md`의 표를 참조. AI 저작 관련 주석/트레일러(`Co-Authored-By`, `Generated by` 등)는 스캔 후 없음을 확인했다 — git 커밋 메시지에만 존재하며 소스 파일 자체에는 포함되지 않는다.
